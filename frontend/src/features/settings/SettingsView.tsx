/** Placeholder settings section — no configurable options yet. */
export default function SettingsView() {
  return (
    <section className="settings">
      <h2>Settings</h2>
      <p className="muted">Nothing to configure yet. Preferences will live here.</p>
    </section>
  );
}
